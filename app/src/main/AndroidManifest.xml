<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muhammetgundogar.himymproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HimymProject">
        <activity android:name=".ScoreActivity"></activity>
        <activity android:name=".TedActivity" />
        <activity android:name=".RobinActivity" />
        <activity android:name=".LilyActivity" />
        <activity android:name=".MarshallActivity" />
        <activity android:name=".BarneyActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>